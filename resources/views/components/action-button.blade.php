<a href="{{ $href }}" class="btn btn-sm btn-{{ $color }}">
    {{-- we added tht double exclamation marks
    so that the edit and the show buttons could render the <i></i> tags --}}
    {!! $text !!}
</a>